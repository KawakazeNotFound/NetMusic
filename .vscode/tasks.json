{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "prepareRunClientCompile",
      "type": "shell",
      "command": "./gradlew prepareRunClientCompile",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "prepareRunClient2Compile",
      "type": "shell",
      "command": "./gradlew prepareRunClient2Compile",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "prepareRunServerCompile",
      "type": "shell",
      "command": "./gradlew prepareRunServerCompile",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}